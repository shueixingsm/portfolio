# CG 作品集网站 · 使用说明

风格：视觉艺术家作品集（GMUNK × Ash Thorp 血统，黑底 / 电影感 / 克制）。
纯静态，**双击 `index.html` 即可打开**，可离线使用。
**中英双语**：右上角 `EN / 中` 点击切换，选择自动记忆。
**已上线**：https://shueixingsm.github.io/portfolio/

---

## 一、信息架构

```
HOME
  HERO          姓名 / VISUAL ARTIST / CG·REAL-TIME VFX·AI FILMMAKING / 上海
  SELECTED WORKS        精选 4 件（张伟的梦 / WORMHOLE / ECHOES OF ORBIT / LIN AIGC）
  EXPLORE BY DISCIPLINE 三大方向入口
  ABOUT strip / CONTACT 页脚

DISCIPLINE 方向页（discipline.html?d=…）
  aifilm        AI 影像 / AIGC   → 原创作品（张伟的梦）+ 实验网格
  rtvfx         实时视效          → VFX Studies 视频网格（悬停播放，点击放大）
  rtcinematics  实时影像          → 案例卡片（点击进案例页）

WORK 案例页（work.html?w=…）
  旗舰（flagship:true）  FILM 分集 / CONCEPT / VISUAL DEVELOPMENT / PROCESS / CREDITS
  普通（case:true）      梗概 / 影片 / 过程章节 / 名单

ABOUT（about.html） 艺术家陈述式：简介 / 方向 / 荣誉
```

## 二、改内容只动 `js/data.js`

字段双语格式 `{ en:"English", zh:"中文" }`；共用的（邮箱、链接、英文作品名）写普通字符串。

### 身份（SITE）
```js
role:     { en: "VISUAL ARTIST", zh: "视觉艺术家" },
subtitle: { en: "CG · REAL-TIME VFX · AI FILMMAKING", zh: "CG · 实时视效 · AI 影像创作" },
location: { en: "SHANGHAI / CHINA", zh: "中国 · 上海" },
```

### 作品三种形态（WORKS 数组）
| 形态 | 字段 | 行为 |
|---|---|---|
| 旗舰案例 | `flagship:true, case:true` | 分集+概念+视觉开发+过程+名单（现为《张伟的梦》） |
| 普通案例 | `case:true` | 案例页（ECHOES / MONOLITH / LIN AIGC 等） |
| 网格条目 | `case:false` + `video:{src,poster}` | 只在网格里悬停播放、点击放大，无案例页 |

- `selected:true` → 上首页精选区（保持 4 件）
- `discipline:"aifilm|rtvfx|rtcinematics"`；aifilm 另有 `group:"original|experiment"`
- `meta` 是网格/卡片下方的技术信息行（如 `"Niagara / Unreal Engine 5"`）——UE 等工具只出现在这一层

### 换上真实素材
1. 视频放 `assets/video/`，封面放 `assets/img/`
2. 改对应条目的 `poster` / `video.src` / `film.src` / `episodes[].src`
3. 《张伟的梦》分集在 `episodes` 数组（EP01 / EP02），概念与过程文案在 `concept` / `process`
4. 不需要的条目整段删掉即可，编号自动重排

## 三、可调视觉
- `css/style.css` 顶部：`--bg`（近黑）/ `--ink`（暖米白）/ `--accent`（暖金）
- 中文排版微调在文件末尾 `body[data-lang="zh"]` 区块
- 首页流光色板在 `js/app.js` 搜 `PALETTES`（blue/gold/cyan）

## 四、更新线上
**找我**：给一枚新 token（Contents/Administration/Pages 读写），我用 `Deliverables\_deploy_gh.ps1` 一键重传。
**自己动手**：github.com → 仓库 shueixingsm/portfolio → Add file → Upload files。
脚本原理：GitHub Contents API，更新已有文件需先 GET 取 sha（脚本已处理）。

## 五、其他
- Netlify 备选：`Deliverables\cg-portfolio-upload.zip` 直接拖 app.netlify.com/drop
- 本地 http 预览：目录内 `powershell -ExecutionPolicy Bypass -File _serve.ps1` → localhost:8137
- 中文走系统字体栈，无额外下载；`prefers-reduced-motion` 自动降级；触屏关自定义光标
