# CG 作品集网站 · 使用说明

风格参考：gmunk.com（首页骨架）× altcinc.com/work/orion_act_1（作品详情页的电影特辑结构）。
纯静态站点，**双击 `index.html` 即可打开**，无需安装任何东西，可离线使用。
**中英双语**：右上角 `EN / 中` 角标点击即切，选择自动记忆（首次访问按浏览器语言自动判断）。

---

## 一、怎么换成你自己的内容（只改 1 个文件）

全站内容集中在 **`js/data.js`**，用任何文本编辑器（记事本 / VS Code）打开。

### 语言规则（重要）
需要翻译的字段写成 `{ en: "English", zh: "中文" }` 双字段；两种语言共用的内容（如邮箱、链接、作品英文标题）直接写普通字符串即可，两种语言都会显示它。

### 1. 换名字 / 头衔 / 邮箱 / 城市
```js
const SITE = {
  name: "Xianglin Qu",                 // 导航/页脚/版权（两种语言通用）
  nameLines: {                         // 首页 hero 大字
    en: ["XIANG LIN", "QU"],           // 英文两行：末行自动空心描边+延伸线
    zh: ["曲相霖"],                     // 中文单行大字+延伸线
  },
  role: { en: "CG Artist & VFX Designer", zh: "CG 艺术家 / 视效设计师" },
  email: "hello@yourname.com",         // ← 你的邮箱
  location: { en: "Shanghai, China", zh: "中国 · 上海" },
  socials: [ ... ],                    // ← 社交链接，不需要的删掉整行
};
```

### 2. 换 / 增 / 删作品
每个作品是 `WORKS` 数组里的一段 `{ ... }`：
- `id`：URL 用的英文代号（如 `lin-aigc-ident`），不要重复、不要空格
- `title`：作品名建议保留英文（国际惯例），其余字段双语
- `featured: true` → 进首页大图区（悬停自动播放视频），否则进「近期作品」行列表
- `poster`：封面图 → 放 `assets/img/`；`film.src`：视频 → 放 `assets/video/`（mp4/webm）
- `synopsis` / `process`（每章 title+text+配图）/ `quote` / `credits`：都写成 `{en, zh}`
- **加作品**：复制一段 `{...}` 改内容；详情页 URL 为 `work.html?w=你的id`
- **删作品**：删掉整段，前后导航自动重排

### 3. 换 About / 能力 / 经历 / 界面文字
- 关于页内容在 `INFO`（同样 `{en, zh}` 格式）
- 界面上的固定文字（导航、按钮、栏目标题等）在 `I18N` 字典的 `en` / `zh` 两组里

---

## 二、目录结构

```
cg-portfolio/
├─ index.html          首页（hero / 精选作品 / 近期作品 / 关于引子 / 页脚）
├─ work.html           作品详情页模板（内容由 ?w=id 从 data.js 渲染）
├─ info.html           关于页（简介 / 能力 / 经历 / 荣誉）
├─ css/style.css       全部样式（配色、字体、双语排版微调都在这里）
├─ js/data.js          ★ 全站内容数据（日常只改这个；含 I18N 界面字典）
├─ js/app.js           交互 / 动效 / 语言切换逻辑（一般不用动）
├─ assets/video/       作品视频
├─ assets/img/         封面图
├─ assets/fonts/       本地字体（Space Grotesk + Instrument Serif；中文走系统字体栈）
├─ assets/vendor/      GSAP / ScrollTrigger / Lenis（已本地化，无需联网）
└─ _serve.ps1          可选：本地预览服务器
```

## 三、可调的视觉细节
```css
/* css/style.css 顶部 */
--bg: #0a0a0b;        /* 背景色（近黑） */
--ink: #eae6df;       /* 主文字色（暖米白） */
--accent: #c8a15a;    /* 点缀色（暖金） */
```
- 中文排版专属微调在 style.css 末尾 `body[data-lang="zh"]` 区块（字距、行高、大字字号）
- 首页流光 shader 色板在 `js/app.js` 搜 `PALETTES`（blue / gold / cyan）

## 四、上线（免费，5 分钟）
任选其一，把整个 `cg-portfolio` 文件夹拖上去即可：
1. **Netlify Drop**（最简单）：app.netlify.com/drop 拖文件夹，立刻得到网址
2. **Vercel / GitHub Pages**：导入文件夹或推仓库开 Pages
3. 自定义域名：平台设置 Custom Domain

## 五、可选：本地预览服务器
双击 `index.html` 已完全够用。如需 http 访问，在目录里执行：
```powershell
powershell -ExecutionPolicy Bypass -File _serve.ps1
# 浏览器访问 http://localhost:8137/
```

## 六、技术说明
- 无构建、无框架、无 CDN 依赖；语言状态存 localStorage，切换即时生效（重载跳过加载动画）
- Lenis 平滑滚动 + GSAP ScrollTrigger + 手写 WebGL 流光 shader；后台标签页加载有 4s 兜底
- 1440 / 768 / 375 三档断点；prefers-reduced-motion 自动降级；触屏自动关自定义光标
- 中文字体走系统字体栈（PingFang SC / Microsoft YaHei），无额外下载体积
